'use client';

import BillingDashboard from '@/components/features/finance/BillingDashboard';

/**
 * BILLING DASHBOARD PAGE
 *
 * This page provides the Finance Officer with a comprehensive view of
 * all student billing data. All metrics are sourced from the StudentBilling
 * collection as the single source of truth.
 */
const BillingDashboardPage = () => {
    return <BillingDashboard />;
};

export default BillingDashboardPage;
